[tool.ruff]
exclude = [
    "acropolis/backup"

]
lint.ignore = [
    "E731", # Do not assign a `lambda` expression, use a `def`
    "E741"  # Ambiguous variable name
]
indent-width = 4

[tool.mypy]
exclude = [
    "acropolis/backup"
]
disable_error_code = [
    "import-untyped"
]
pretty = true